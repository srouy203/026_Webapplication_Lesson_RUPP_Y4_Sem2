{
   "configurations": [
   {
      "name": "Launch Chrome",
      "request": "launch",
      "type": "chrome",
      "url": "http://localhost:8080/E5Project/${relativeFile}",
      "webRoot": "${workspaceFolder}"
   }
   ]
}